# boshpana

## Notes

- Future feature: verify `contact.user_id` matches the sender before saving the phone number.
- Future feature: derive district and eventually the address from given gps locations.
